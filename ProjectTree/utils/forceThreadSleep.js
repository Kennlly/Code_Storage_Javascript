//node v18+
// import { setTimeout, setImmediate, setInterval } from "timers/promises";
// await setTimeout(2000);

//old version
// export default function forceThreadSleep(ms) {
//    return new Promise((resolve) => setTimeout(() => resolve(), ms));
// }
